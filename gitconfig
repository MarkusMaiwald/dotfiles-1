[core]
  editor = vim -f
[color]
  diff = auto
  interactive = always
  branch = auto
  status = auto
[user]
  name = Will St. Clair
  email = will@willstclair.com
[filter "gpg"]
  smudge = gpg -d -q --batch --no-tty
  clean  = gpg -ea -q --batch --no-tty -r A4D118F7
[diff "gpg"]
  textconv = decrypt
[github]
  user = wsc
